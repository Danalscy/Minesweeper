/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package treetest;

import java.io.Console;
import java.io.File;
import java.io.FileNotFoundException;
import java.util.HashSet;
import java.util.Scanner;
import java.util.Set;

/**
 *
 * @author nguyend1
 */
public class Treetest {

    /**
     * @param args the command line arguments
     */
   
    public static void main(String[] args) {
        // TODO code application logic here
        /* Vector v = new Vector();
        v.Przypisz(5,5);
        v.Przypisz(2,2);
        v.Przypisz(-4,-4);
        v.Przypisz(3,3);
        v.Przypisz(18,18);
        v.Przypisz(21,21);
        v.Przypisz(19,19);
        v.Przypisz(25,25);
        System.out.println("Wektor v:");
        v.dump();
        v.remove(18);
        System.out.println("\nPo wykasowaniu 18:");
        v.dump();

        Vector w = new Vector();
        w.Przypisz(5,5);
        w.Przypisz(2,2);
        w.Przypisz(-4,-4);
        w.Przypisz(3,3);
        w.Przypisz(12,12);
        w.Przypisz(9,9);
        w.Przypisz(21,21);
        w.Przypisz(19,19);
        w.Przypisz(25,25);
        System.out.println("Wektor w:");
        w.dump();
        w.remove(12);
        System.out.println("\nPo wykasowaniu 12:");
        w.dump();
        System.out.println("\n");
       
        System.out.println("Sciezka do A: ");
        Scanner scanner = new Scanner(System.in);
        String a = scanner.nextLine();
        System.out.println("Sciezka do B: ");
        String b = scanner.nextLine();
        Menu menu = new Menu(a,b);
        menu.getA().show();
        menu.getB().show();
        System.out.println("+ - dodawanie");
        System.out.println("* - mnozenie");
        System.out.println("T - Transpozycja");
        System.out.println("s - pokaz macierze");
        System.out.println("h - help");
        System.out.println("q - zakoncz program");
        while(true){
            char c = scanner.next().charAt(0);
            if(c=='+'){
                menu.addition().show();
            }
            if(c=='*'){
                 menu.multiply().show();
             }
            if(c=='T'){
                 menu.getA().transpose().show();
                 menu.getB().transpose().show();
             }
            if(c=='s'){
                 menu.getA().show();
                 menu.getB().show();
            }
            if(c=='h'){
                 System.out.println("+ - dodawanie");
                 System.out.println("* - mnozenie");
                 System.out.println("T - Transpozycja");
                 System.out.println("s - pokaz macierze");
                 System.out.println("h - help");
                 System.out.println("q - zakoncz program");
            }
            if(c=='q'){
                 break;
            }
        }
        scanner.close();  */
    }
}
